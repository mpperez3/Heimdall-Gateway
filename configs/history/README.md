# Histórico de configuraciones Heimdall Gateway

Cada fichero es un snapshot de `catalog.json` + `config.yaml` para un modelo.

Formato: `YYYY-MM-DD_HHMM_modelo_ctx_split_parallel.json`

El `.gitignore` del repo ignora `/.history/` pero **sí** versiona `configs/history/` (histórico curado).
